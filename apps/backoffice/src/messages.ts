import { defineMessages } from "@purosur/ui";

// The backoffice's own message catalog. It lives only in the backoffice and never imports from
// the register's catalog.
export const messages = defineMessages("es-AR", () => ({}));
