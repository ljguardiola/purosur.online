import { defineHelp } from "@purosur/ui";

// The backoffice's own help content. It lives only in the backoffice and never imports from the
// register's help content.
export const help = defineHelp("es-AR", { categories: {}, articles: {} });
