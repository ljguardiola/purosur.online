import { defineMessages } from "@purosur/ui";

// The register's own message catalog. It ships inside the register's installer and never
// imports from the backoffice's catalog, so it stays available offline.
export const messages = defineMessages("es-AR", () => ({}));
